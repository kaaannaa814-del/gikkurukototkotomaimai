/* 全体リセット */
* {
  margin: 0;
  padding: 0;
  box-sizing: border-box;
}

/* 背景設定 */
body {
  min-height: 100vh;
  font-family: "Arial", sans-serif;
  text-align: center;
  color: white;
  background: url("https://raw.githubusercontent.com/kaaannaa814-del/gikkurukototkotomaimai/main/images/snowboard115.jpg")
    no-repeat center center / cover;
  display: flex;
  align-items: center;
  justify-content: center;

  font-size: 20px; /* ✅ 全体の基本文字サイズUP */
}

/* メインコンテナ（ガラス風） */
.container {
  width: 90%;
  max-width: 900px;
  background: rgba(0, 0, 0, 0.45);
  backdrop-filter: blur(8px);
  border-radius: 20px;
  padding: 55px 40px; /* ✅ 少しゆったり */
  box-shadow: 0 10px 30px rgba(0, 0, 0, 0.4);
}

/* ✅ タイトル（さらにドーンと） */
h1 {
  font-size: 84px;  /* ✅ さらに大きく */
  font-weight: bold;
  letter-spacing: 3px;
  margin-bottom: 40px;
}

/* ナビゲーション */
nav {
  display: flex;
  justify-content: center;
  gap: 30px;
  margin-bottom: 50px;
  flex-wrap: wrap;
}

/* ✅ ボタン（はっきり大きく） */
nav a {
  text-decoration: none;
  color: white;
  border: 2px solid white;
  padding: 18px 36px;
  border-radius: 40px;
  transition: 0.3s;
  font-weight: bold;
  font-size: 22px; /* ✅ しっかり大きく */
}

nav a:hover {
  background: white;
  color: black;
}

/* セクション共通 */
section {
  margin-top: 55px;
}

section h2 {
  font-size: 46px; /* ✅ 見出しUP */
  margin-bottom: 22px;
}

section p,
section li {
  font-size: 24px; /* ✅ 本文UP */
  line-height: 2;
}

ul {
  list-style: none;
  padding: 0;
}

/* 📱 スマホ最適化（しっかり大きく） */
@media screen and (max-width: 768px) {
  .container {
    padding: 40px 24px;
  }

  h1 {
    font-size: 52px; /* ✅ スマホでもドーン */
    letter-spacing: 2px;
  }

  section h2 {
    font-size: 34px;
  }

  section p,
  section li {
    font-size: 20px;
  }

  nav {
    gap: 16px;
  }

  nav a {
    padding: 14px 26px;
    font-size: 18px;
  }
}
